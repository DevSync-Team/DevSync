import { InvitationScreen } from '@/screens'
import React, { Suspense } from 'react'

const InvatationPage = () => {
  return (
    <Suspense>
        <InvitationScreen/>
    </Suspense>
  )
}

export default InvatationPage