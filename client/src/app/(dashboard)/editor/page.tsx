
import { EditorScreen } from '@/screens'
import React, { Suspense } from 'react'

const EditorPage = () => {
  return (
  <Suspense>
    <EditorScreen/>
  </Suspense>
  )
}

export default EditorPage